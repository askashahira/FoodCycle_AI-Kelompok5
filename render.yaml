services:
  - type: web
    name: foodcycle-ai
    env: python

    buildCommand: |
      pip install -r requirements.txt &&
      python manage.py collectstatic --noinput &&
      python manage.py migrate

    startCommand: |
      python manage.py createadmin &&
      python manage.py seeddata &&
      gunicorn foodcycle.wsgi